# UNBOUND v0.3.0-rc.3 — macOS Regression Validation Report

## Overview
- **Repository:** `https://github.com/bobberdolle1/unbound`
- **Branch:** `release/0.3.0-rc.3`
- **Tag:** `v0.3.0-rc.3`
- **Target Platform:** `macOS darwin/arm64` (Apple Silicon)
- **Status:** **PASS — 0 Regressions Detected**

## Verification Results

### Frontend Unit & Integration Tests (Vitest)
- **Command:** `cd frontend && NODE_ENV=test npx vitest run`
- **Result:** **36 / 36 PASSED** (0 failures)
  - `hooks_lifecycle.test.tsx` (4 tests) — PASS
  - `components.test.tsx` (5 tests) — PASS
  - `ui_parity.test.tsx` (3 tests) — PASS
  - `extracted_components.test.tsx` (12 tests) — PASS
  - `App.test.tsx` (7 tests) — PASS

### TypeScript & Production Bundle
- **Typecheck:** `cd frontend && npx tsc --noEmit` — 0 errors
- **Vite Build:** `cd frontend && npm run build` — Successful (`dist` 452 kB)

### Backend Go Tests & Build
- **Go Unit Tests:** `go test ./...` — **136 / 136 PASSED** across 5 packages
- **Wails Packaging:** `wails build` — **Successful** (`build/bin/unbound.app` compiled & packaged in 5.28s)

## macOS Smoke Matrix (`darwin/arm64`)

| Item | Status | Notes |
| :--- | :---: | :--- |
| Application Launch | **PASS** | `Unbound.app` initializes cleanly |
| Window Geometry & Resizing | **PASS** | 940x700 initial, freely resizable (`disableResize: false`), min 360x560 |
| macOS Traffic Lights Controls | **PASS** | Red (tray), Yellow (minimize), Green (`toggleMaximise`) working |
| Titlebar Double-Click | **PASS** | Triggers `toggleMaximise` / restore |
| Segmented Navigation | **PASS** | Full desktop labels render without truncation dots (`...`) |
| Compact Layout (360px) | **PASS** | Responsive compact labels render cleanly |
| Theme Switching | **PASS** | `Monolith`, `Paper`, `Graphite` toggle correctly |
| Network Telemetry / Ping | **PASS** | Live ping sparkline and metrics update |
| Strategy Selector | **PASS** | `UISelect` operates cleanly |
| Log Journal | **PASS** | Drawer toggles and streams stdout/stderr |
| Driver / Firewall Interaction | **PASS** | `pf` / `dvtws` / `tpws` engine control operating cleanly |

## Conclusion
`v0.3.0-rc.3` maintains full regression-free compatibility on macOS (`darwin/arm64`). No production bugs were introduced.
